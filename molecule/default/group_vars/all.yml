---
web_server:
  package: nginx
  service: nginx
  port: 80

database_server:
  package: mariadb-server
  service: mariadb
  port: 3306

test_files:
  - path:  /opt/assert_file
    state: touch
  - path:  /opt/assert_directory
    state: directory
  - path:  /opt/assert_link
    state: link
    src: /usr/local/bin

    

