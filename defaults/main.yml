---
assert_package:
  name: sudo
  ### Option ###
  ## values: true / false (default: true)
  # is_installed: true

assert_group:
  name: root
  ### Option ###
  ## values: true / false (default: true)
  # exist: true

assert_user:
  name: root
  ## Option
  ## values: true / false (default: true)
  # exist: true

assert_service:
  name: httpd
  ### Option ###
  ## values: true / false (default: true)
  # is_running: true
  ## values: true / false (default: true)
  # is_enabled: null

assert_port:
  port: 80
  ### Option ###
  ## values: true / false (default: true)
  # is_listening: true
  ## values: tcp / tcp6 / udp / udp6 (default: null)
  # protocol: null
