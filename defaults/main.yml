---
assert_package:
  name: sudo
  ### Option ###
  ## values: true / false (default: true)
  # is_installed: true

assert_group:
  name: root
  ### Option ###
  ## values: true / false (default: true)
  # exists: true

assert_user:
  name: root
  ## Option
  ## values: true / false (default: true)
  # exists: true

assert_service:
  name: httpd
  ### Option ###
  ## values: true / false (default: true)
  # is_running: true
  ## values: true / false (default: true)
  # is_enabled: null

assert_port:
  port: 80
  ### Option ###
  ## values: true / false (default: true)
  # is_listening: true
  ## values: tcp / tcp6 / udp / udp6 (default: null)
  # protocol: null

assert_file:
  path: /root
  ### Option ###
  ## values: true / false (default: true)
  # exists: true
  ## values: true / false (default: null)
  # is_dir: null
  ## values: true / false (default: null)
  # is_file: null
  ## values: true / false (default: null)
  # is_symlink: null
  ## values: "0755" (default: null)
  # mode: null
  ## values: "root" (default: null)
  # owner: null
  ## values: "root" (default: null)
  # group: null
